<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/jeecrud</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">toor</property>

        <!-- PostgreSQL-specific dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Enable automatic schema update -->

        <property name="hibernate.hbm2ddl.auto">create-drop</property>

        <!-- SQL output settings -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.use_sql_comments">true</property>

        <!-- Connection pool settings (using built-in Hibernate pool) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Current session context -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Disable second-level cache -->
        <property name="hibernate.cache.use_second_level_cache">false</property>

        <!-- Specify hibernate.jdbc.time_zone to ensure consistent timezone handling -->
        <property name="hibernate.jdbc.time_zone">UTC</property>

        <!-- Enable batch processing -->
        <property name="hibernate.jdbc.batch_size">50</property>
        <property name="hibernate.order_inserts">true</property>
        <property name="hibernate.order_updates">true</property>

        <!-- Mapping your entity classes -->
        <mapping class="org.devsyc.domain.entities.User"/>
        <mapping class="org.devsyc.domain.entities.Task"/>
        <mapping class="org.devsyc.domain.entities.TaskRequest"/>
    </session-factory>
</hibernate-configuration>